---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: requestedbackupactions.riotkit.org
spec:
  group: riotkit.org
  names:
    kind: RequestedBackupAction
    listKind: RequestedBackupActionList
    plural: requestedbackupactions
    singular: requestedbackupaction
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RequestedBackupAction is the Schema for the requestedbackupactions
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RequestedBackupActionSpec defines the desired state of RequestedBackupAction
            properties:
              action:
                enum:
                - backup
                - restore
                type: string
              scheduledBackupRef:
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              targetVersion:
                type: string
            required:
            - action
            - scheduledBackupRef
            type: object
          status:
            description: RequestedBackupActionStatus defines the observed state of
              RequestedBackupAction
            properties:
              processed:
                type: boolean
            required:
            - processed
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
