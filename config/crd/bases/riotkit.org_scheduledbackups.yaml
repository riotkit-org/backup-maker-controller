---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: scheduledbackups.riotkit.org
spec:
  group: riotkit.org
  names:
    kind: ScheduledBackup
    listKind: ScheduledBackupList
    plural: scheduledbackups
    singular: scheduledbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Cron expression
      jsonPath: .spec.cronjob.scheduleEvery
      name: Schedule
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ScheduledBackup is the Schema for the scheduledbackups API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ScheduledBackupSpec defines the desired state of ScheduledBackup
            properties:
              collectionId:
                type: string
              cronJob:
                properties:
                  enabled:
                    type: boolean
                  scheduleEvery:
                    default: 00 02 * * *
                    type: string
                required:
                - enabled
                type: object
              gpgKeySecretRef:
                description: GPGKeySecretSpec represents .spec.gpgKeySecretRef section
                properties:
                  createIfNotExists:
                    type: boolean
                  email:
                    type: string
                  passphraseKey:
                    type: string
                  privateKey:
                    type: string
                  publicKey:
                    type: string
                  secretName:
                    type: string
                required:
                - createIfNotExists
                - email
                - passphraseKey
                - privateKey
                - publicKey
                - secretName
                type: object
              operation:
                enum:
                - backup
                - restore
                type: string
              templateRef:
                description: TemplateSpec represents .spec.templateRef section
                properties:
                  kind:
                    type: string
                  name:
                    type: string
                required:
                - kind
                - name
                type: object
              tokenSecretRef:
                description: TokenSecretSpec represents .spec.tokenSecretRef
                properties:
                  secretName:
                    type: string
                  tokenKey:
                    type: string
                required:
                - secretName
                - tokenKey
                type: object
              vars:
                description: VarsSpec represents .spec.vars - a hashmap of values
                  applied to template's backup & restore scripts
                type: string
              varsSecretRef:
                description: VarsSecretSpec represents .spec.varsSecretRef
                properties:
                  importOnlyKeys:
                    items:
                      type: string
                    type: array
                  secretName:
                    type: string
                type: object
            required:
            - collectionId
            - cronJob
            - gpgKeySecretRef
            - operation
            - templateRef
            - tokenSecretRef
            - vars
            - varsSecretRef
            type: object
          status:
            description: ScheduledBackupStatus defines the observed state of ScheduledBackup
            properties:
              configmapStatus:
                type: boolean
              secretStatus:
                type: boolean
              workloadStatus:
                description: "todo: store there a some kind of hash as a last applied
                  configuration id to know that the objects were already applied the
                  hash should be calculated from the CRD properties \n todo: add history
                  of conditions like the Deployment has"
                type: boolean
            required:
            - configmapStatus
            - secretStatus
            - workloadStatus
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
